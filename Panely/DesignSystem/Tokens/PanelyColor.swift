import SwiftUI

enum PanelyColor {
    static let bgPrimary = Color(hex: 0x0F1115)
    static let bgSecondary = Color(hex: 0x1C1F26)
    static let bgTertiary = Color(hex: 0x2A2E36)

    static let textPrimary = Color(hex: 0xE6EAF2)
    static let textSecondary = Color(hex: 0xAAB2C0)

    static let borderSubtle = Color.white.opacity(0.08)

    static let accentPrimary = Color(hex: 0x0A84FF)
}
